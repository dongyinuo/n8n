# ============================================
# n8n 环境变量配置示例
# ============================================
# 复制此文件为 .env 并填入实际值
# 注意：此文件不应包含真实的 API 密钥

# ============================================
# 开发环境配置
# ============================================

# 许可证绕过（仅用于开发环境）
# 设置为 true 可在开发环境中绕过许可证检查
N8N_BYPASS_LICENSE=true

# ============================================
# AI Builder 模型配置
# ============================================
# AI Workflow Builder 功能需要配置以下环境变量

# 模型提供商
# 可选值: 'anthropic' (默认) | 'openai'
# - 'anthropic': 使用 Anthropic Claude 模型
# - 'openai': 使用 OpenAI 兼容的 API（包括 OpenAI 官方 API 或其他兼容服务）
N8N_AI_BUILDER_PROVIDER=anthropic

# 模型名称（根据提供商选择）
# Anthropic 模型示例:
#   - claude-sonnet-4-5 (默认)
#   - claude-3-5-sonnet-20241022
#   - claude-3-opus-20240229
# OpenAI 模型示例:
#   - gpt-4
#   - gpt-4-turbo
#   - gpt-3.5-turbo
#   - gpt-4o-mini
N8N_AI_BUILDER_MODEL=claude-sonnet-4-5

# API 密钥配置
# 根据选择的提供商设置对应的 API 密钥：
# - 使用 OpenAI: 设置 N8N_AI_BUILDER_API_KEY
# - 使用 Anthropic: 设置 N8N_AI_ANTHROPIC_KEY
#   如果同时设置了 N8N_AI_BUILDER_API_KEY，则优先使用 N8N_AI_BUILDER_API_KEY

# OpenAI API 密钥（用于 OpenAI 提供商）
# 获取方式: https://platform.openai.com/api-keys
#N8N_AI_BUILDER_API_KEY=sk-your-openai-api-key-here

# Anthropic API 密钥（用于 Anthropic 提供商）
# 获取方式: https://console.anthropic.com/
# 当 N8N_AI_BUILDER_PROVIDER=anthropic 且 N8N_AI_BUILDER_API_KEY 未设置时使用
#N8N_AI_ANTHROPIC_KEY=sk-ant-your-anthropic-api-key-here

# 自定义 API Base URL（可选）
# 默认值会根据提供商自动设置：
# - OpenAI: https://api.openai.com/v1
# - Anthropic: 使用官方 API（无需设置）
# 如果需要使用自定义 API 端点（如代理服务器），可以设置此变量
#N8N_AI_BUILDER_BASE_URL=https://api.openai.com/v1

# ============================================
# AI Assistant 服务配置（可选）
# ============================================
# 如果使用企业版 AI Assistant 服务，需要配置以下变量

# AI Assistant 服务地址
# 仅在使用企业版 AI Assistant 服务时需要设置
#N8N_AI_ASSISTANT_BASE_URL=https://your-ai-assistant-service.com
